# ShoppingList
MEAN stack web app test development.

##Build and Run Server
1. Run `npm install` to install node dependencies.
2. (Optional) Install `npm install -g nodemon` for running node with change monitoring. After it has finished downloading, run `nodemon` in **./source/crud-backend**.
3. Run `node entry.js` in **./source/crud-backend**.
